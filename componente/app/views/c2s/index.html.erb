<h1>Listing c2s</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @c2s.each do |c2| %>
  <tr>
    <td><%= link_to 'Show', c2 %></td>
    <td><%= link_to 'Edit', edit_c2_path(c2) %></td>
    <td><%= link_to 'Destroy', c2, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New C2', new_c2_path %>
